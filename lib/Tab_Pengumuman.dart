import 'package:flutter/material.dart';

class TabPengumuman extends StatelessWidget {
  const TabPengumuman({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('data'),),
    );
  }
}